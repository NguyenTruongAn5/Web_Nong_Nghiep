@model X.PagedList.IPagedList<PostModel>
@using X.PagedList.Mvc.Core
@{
    ViewData["title"] = "Restore Post Title";
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Title</th>
            <th scope="col">Content</th>
            <th scope="col">Author </th>
            <th scope="col">CreatedAt </th>
            <th scope="col">Restore</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Title</td>
                <td>
                    <div class="description-truncate" title="@item.Content">
                        @item.Content
                    </div>
                </td>
                <td>@item.UserID</td>
                <td>@item.CreatedAt</td>
                <td>
                    <a asp-action="ExcuteRestore" class="btn btn-warning btn-sm" asp-controller="Post" asp-route-Id="@item.Id">Restore</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    @Html.PagedListPager(Model, page=>Url.ActionLink("Index", "Post", new{page=page}))
</div>

